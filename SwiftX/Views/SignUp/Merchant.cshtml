@{
    ViewData["Title"] = "Sign Up";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

@model SwiftX.Models.MerchantModel
<section class="px-4 py-10 md:py-16 bg-[var(--dark-bg-color)] text-[var(--light-text-color)]">
    <form method="post"
          asp-action="SignUpMerchant"
          asp-controller="SignUp"
          enctype="multipart/form-data"
          id="merchantSignupForm"
          novalidate>

        @Html.AntiForgeryToken()

        <div class="max-w-xl mx-auto">

            <!-- ── Header ── -->
            <div class="text-center mb-8 md:mb-10">
                <h1 class="__h1 text-[var(--primary-color)]">Merchant Sign Up</h1>
                <p class="__body mt-2 opacity-60">Register your business and start selling with SwiftX.</p>
            </div>

            <!-- ── Step Indicator ── -->
            <div class="flex items-center justify-center mb-10 px-2">
                <div class="flex flex-col items-center gap-1.5">
                    <div class="step-circle" data-step-circle="1">1</div>
                    <span class="step-label" data-step-label="1">Basic Info</span>
                </div>
                <div class="step-connector" data-connector="1"></div>
                <div class="flex flex-col items-center gap-1.5">
                    <div class="step-circle" data-step-circle="2">2</div>
                    <span class="step-label" data-step-label="2">Documents</span>
                </div>
                <div class="step-connector" data-connector="2"></div>
                <div class="flex flex-col items-center gap-1.5">
                    <div class="step-circle" data-step-circle="3">3</div>
                    <span class="step-label" data-step-label="3">Login Info</span>
                </div>
            </div>

            <!-- ── All Sections ── -->
            <div class="space-y-5">

                <!-- ════════════════════════════
                     SECTION 1 · Basic Info
                ════════════════════════════ -->
                <div class="__signup-card" id="section-1">
                    <div class="flex items-center gap-3 mb-4">
                        <div class="step-circle step-circle--card" id="card-circle-1">1</div>
                        <h2 class="__h4">Basic Info</h2>
                    </div>

                    <p class="__small opacity-80 mb-1">Enter your business and owner details.</p>
                    <p class="__micro text-[var(--primary-color)] mb-5">*All fields required.</p>

                    <!-- Business Name -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Business Name</label>
                        <div class="input-icon-wrap">
                            <input type="text" class="signup-input" id="inp-businessname"
                                   asp-for="BusinessName" placeholder="Business name" autocomplete="organization" />
                            <span class="input-icon"><i class="ph ph-storefront"></i></span>
                        </div>
                        <span class="field-error" id="err-businessname"></span>
                    </div>

                    <!-- Owner First Name -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Owner First Name</label>
                        <div class="input-icon-wrap">
                            <input type="text" class="signup-input" id="inp-ownerfirst"
                                   asp-for="OwnerFirstName" placeholder="First name" autocomplete="given-name" />
                            <span class="input-icon"><i class="ph ph-user"></i></span>
                        </div>
                        <span class="field-error" id="err-ownerfirst"></span>
                    </div>

                    <!-- Owner Last Name -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Owner Last Name</label>
                        <div class="input-icon-wrap">
                            <input type="text" class="signup-input" id="inp-ownerlast"
                                   asp-for="OwnerLastName" placeholder="Last name" autocomplete="family-name" />
                            <span class="input-icon"><i class="ph ph-user"></i></span>
                        </div>
                        <span class="field-error" id="err-ownerlast"></span>
                    </div>

                    <!-- Business Address -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Business Address</label>
                        <div class="input-icon-wrap">
                            <input type="text" class="signup-input" id="inp-bizaddress"
                                   asp-for="BusinessAddress" placeholder="Complete business address" autocomplete="street-address" />
                            <span class="input-icon"><i class="ph ph-map-pin"></i></span>
                        </div>
                        <span class="field-error" id="err-bizaddress"></span>
                    </div>

                    <!-- Business Contact -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Business Contact No.</label>
                        <div class="input-icon-wrap">
                            <input type="tel" class="signup-input" id="inp-bizcontact"
                                   asp-for="BusinessContact" placeholder="09XX XXX XXXX" autocomplete="tel" />
                            <span class="input-icon"><i class="ph ph-phone"></i></span>
                        </div>
                        <span class="field-error" id="err-bizcontact"></span>
                    </div>

                    <!-- Business Email -->
                    <div class="mb-2">
                        <label class="__label block mb-1.5">*Business Email Address</label>
                        <div class="input-icon-wrap">
                            <input type="email" class="signup-input" id="inp-bizemail"
                                   asp-for="BusinessEmail" placeholder="business@email.com" autocomplete="email" />
                            <span class="input-icon"><i class="ph ph-envelope-simple"></i></span>
                        </div>
                        <span class="field-error" id="err-bizemail"></span>
                    </div>
                </div>


                <!-- ════════════════════════════
                     SECTION 2 · Documents
                ════════════════════════════ -->
                <div class="__signup-card" id="section-2">
                    <div class="flex items-center gap-3 mb-4">
                        <div class="step-circle step-circle--card" id="card-circle-2">2</div>
                        <h2 class="__h4">Requirements &amp; Documents</h2>
                    </div>

                    <p class="__small opacity-80 mb-1">Upload clear, readable copies of your documents.</p>
                    <p class="__micro text-[var(--primary-color)] mb-5">*Required fields marked. JPG, PNG, PDF — max 10 MB each.</p>

                    <div class="upload-group">

                        <!-- BIR Form 2303 (required) -->
                        <div class="upload-card">
                            <div class="upload-card__icon"><i class="ph ph-file-text"></i></div>
                            <div class="upload-card__info">
                                <span class="__label block">*BIR Form 2303</span>
                                <p class="upload-card__hint">Certificate of Registration</p>
                            </div>
                            <div class="su-file-upload" id="fu-bir">
                                <input type="file" id="inp-bir" asp-for="BIRForm"
                                       accept=".jpg,.jpeg,.png,.pdf" class="su-file-input" />
                                <div class="su-file-label" aria-hidden="true">
                                    <i class="ph ph-upload-simple"></i>
                                    <span>Choose file</span>
                                </div>
                            </div>
                            <span class="field-error w-full" id="err-bir"></span>
                        </div>

                        <!-- DTI Certificate (optional) -->
                        <div class="upload-card">
                            <div class="upload-card__icon"><i class="ph ph-seal-check"></i></div>
                            <div class="upload-card__info">
                                <span class="__label block opacity-70">DTI Business Name Certificate <span class="__micro opacity-60">(optional)</span></span>
                                <p class="upload-card__hint">Recommended but not required</p>
                            </div>
                            <div class="su-file-upload" id="fu-dti">
                                <input type="file" id="inp-dti" asp-for="DTICertificate"
                                       accept=".jpg,.jpeg,.png,.pdf" class="su-file-input" />
                                <div class="su-file-label" aria-hidden="true">
                                    <i class="ph ph-upload-simple"></i>
                                    <span>Choose file</span>
                                </div>
                            </div>
                            <!-- no error span — optional field -->
                        </div>

                        <!-- Barangay Clearance (required) -->
                        <div class="upload-card">
                            <div class="upload-card__icon"><i class="ph ph-buildings"></i></div>
                            <div class="upload-card__info">
                                <span class="__label block">*Barangay Business Clearance</span>
                                <p class="upload-card__hint">For small / home-based businesses</p>
                            </div>
                            <div class="su-file-upload" id="fu-barangay">
                                <input type="file" id="inp-barangay" asp-for="BarangayClearance"
                                       accept=".jpg,.jpeg,.png,.pdf" class="su-file-input" />
                                <div class="su-file-label" aria-hidden="true">
                                    <i class="ph ph-upload-simple"></i>
                                    <span>Choose file</span>
                                </div>
                            </div>
                            <span class="field-error w-full" id="err-barangay"></span>
                        </div>

                    </div><!-- /upload-group -->
                    <!-- Payment Info -->
                    <div class="upload-divider"><span>Payment Info</span></div>
                    <div class="mb-2">
                        <label class="__label block mb-1.5">*GCash Number</label>
                        <div class="input-icon-wrap">
                            <input type="tel" class="signup-input" id="inp-gcash"
                                   asp-for="GCContact" placeholder="09XX XXX XXXX" />
                            <span class="input-icon"><i class="ph ph-wallet"></i></span>
                        </div>
                        <span class="field-error" id="err-gcash"></span>
                    </div>
                </div>


                <!-- ════════════════════════════
                     SECTION 3 · Account Setup
                ════════════════════════════ -->
                <div class="__signup-card" id="section-3">
                    <div class="flex items-center gap-3 mb-4">
                        <div class="step-circle step-circle--card" id="card-circle-3">3</div>
                        <h2 class="__h4">Account Setup</h2>
                    </div>

                    <p class="__small opacity-80 mb-1">Create your merchant login credentials.</p>
                    <p class="__micro text-[var(--primary-color)] mb-5">*All fields required.</p>

                    <!-- Username -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Username</label>
                        <div class="input-icon-wrap">
                            <input type="text" class="signup-input" id="inp-username"
                                   asp-for="Username" placeholder="Username" autocomplete="username" />
                            <span class="input-icon"><i class="ph ph-at"></i></span>
                        </div>
                        <span class="field-error" id="err-username"></span>
                    </div>

                    <!-- Email -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Email Address</label>
                        <div class="input-icon-wrap">
                            <input type="email" class="signup-input" id="inp-email"
                                   asp-for="Email" placeholder="Email address" autocomplete="email" />
                            <span class="input-icon"><i class="ph ph-envelope-simple"></i></span>
                        </div>
                        <span class="field-error" id="err-email"></span>
                    </div>

                    <!-- Password -->
                    <div class="mb-4">
                        <label class="__label block mb-1.5">*Password</label>
                        <div class="password-wrapper input-icon-wrap">
                            <input type="password" class="signup-input" id="inp-password"
                                   asp-for="Password" placeholder="Password" autocomplete="new-password" />
                            <span class="input-icon"><i class="ph ph-lock-simple"></i></span>
                            <button type="button" class="password-toggle" data-toggle-pw>
                                <svg class="w-5 h-5 eye-open" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                </svg>
                                <svg class="w-5 h-5 eye-closed hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.878 9.878L6.59 6.59m7.532 7.532l3.29 3.29M3 3l18 18" />
                                </svg>
                            </button>
                        </div>
                        <span class="field-error" id="err-password"></span>
                    </div>

                    <!-- Confirm Password -->
                    <div class="mb-8">
                        <label class="__label block mb-1.5">*Confirm Password</label>
                        <div class="password-wrapper input-icon-wrap">
                            <input type="password" class="signup-input" id="inp-confirm"
                                   placeholder="Confirm password" autocomplete="new-password" />
                            <span class="input-icon"><i class="ph ph-lock-simple"></i></span>
                            <button type="button" class="password-toggle" data-toggle-pw>
                                <svg class="w-5 h-5 eye-open" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                </svg>
                                <svg class="w-5 h-5 eye-closed hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.878 9.878L6.59 6.59m7.532 7.532l3.29 3.29M3 3l18 18" />
                                </svg>
                            </button>
                        </div>
                        <span class="field-error" id="err-confirm"></span>
                    </div>

                    <!-- Submit -->
                    <button type="submit" class="btn-next" id="btn-submit">
                        Submit Application
                    </button>
                    <p class="__micro text-center opacity-40 mt-4">
                        By submitting, you agree to SwiftX's Terms of Service and Privacy Policy.
                    </p>
                </div>

            </div><!-- /space-y-5 -->
        </div><!-- /max-w-xl -->
    </form>
</section>

@section PageScripts {
    <script src="~/js/merchant_signup.js"></script>
    @if (!ViewData.ModelState.IsValid)
    {
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                @if (ViewData.ModelState.TryGetValue("Username", out var uname) && uname.Errors.Count > 0)
                {
                    <text>
                    var eU = document.getElementById('err-username');
                    if(eU) { eU.textContent = '@Html.Raw(uname.Errors[0].ErrorMessage)'; eU.classList.add('visible'); document.getElementById('inp-username').style.borderColor = '#ef4444'; }
                    </text>
                }
                @if (ViewData.ModelState.TryGetValue("Email", out var email) && email.Errors.Count > 0)
                {
                    <text>
                    var eE = document.getElementById('err-email');
                    if(eE) { eE.textContent = '@Html.Raw(email.Errors[0].ErrorMessage)'; eE.classList.add('visible'); document.getElementById('inp-email').style.borderColor = '#ef4444'; }
                    </text>
                }
            });
        </script>
    }
}
